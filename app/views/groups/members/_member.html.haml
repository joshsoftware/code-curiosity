%tr
  %td= member_counter + 1
  %td= member.name || '-'
  %td= link_to member.github_handle, github_url(member), target: '_blank'
  %td
    - if group_owner?(member)
      ADMIN
  %td
    - if @group.can_remove_member?(member, current_user) 
      = link_to  group_member_path(@group, member), class: 'btn btn-xs btn-danger', data: { confirm: 'Are you sure?'}, remote: true, method: 'delete' do
        %i.fa.fa-times
        = group_owner?(current_user) ? 'Remove' : 'Leave'
