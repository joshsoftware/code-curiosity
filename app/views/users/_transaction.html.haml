%tr
  %td= transaction_counter + 1
  %td= transaction.points
  %td
    %b
      = transaction.transaction_type.humanize
    %small.label{class: transaction.credit? ? 'bg-green' : 'bg-red'}
      = transaction.type.upcase
  %td= transaction.created_at.strftime('%D %T')
